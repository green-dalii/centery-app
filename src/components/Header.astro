---
export interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="bg-primary text-primary-content p-4 sticky top-0 z-10">
    <h1 class="text-xl font-bold text-center">{title}</h1>
</header>